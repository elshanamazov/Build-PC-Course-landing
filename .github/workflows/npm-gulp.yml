name: Gulp CI

on:
  push:
    branches:
      - main # Измените на вашу основную ветку

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14' # Измените на желаемую версию Node.js

      - name: Install project dependencies
        run: npm install

      - name: Build with Gulp
        run: gulp build # Замените на ваш таск для сборки проекта

      # В этом месте вы можете добавить дополнительные шаги, например, тестирование или деплой
Вам также понадобится файл gulpfile.js в корне вашего проекта, который содержит задачи для Gulp, такие как сборка проекта. В этом файле вы можете определить различные задачи, которые GitHub Actions будет выполнять.
Пример простого gulpfile.js:

js
Copy code
const gulp = require('gulp');

gulp.task('build', function() {
  // Ваш код для сборки проекта
});
Зафиксируйте и отправьте файлы .github/workflows/gulp-ci.yml и gulpfile.js в ваш репозиторий на GitHub.
Теперь, при каждом push'е в ветку main (в данном примере), GitHub Actions будет автоматически выполнять указанные шаги, включая установку зависимостей проекта и запуск Gulp для сборки. Вы также можете добавить дополнительные шаги, такие как тестирование или деплой, чтобы полностью автоматизировать процесс CI/CD вашего проекта.




User
Предоставь полный код deploy 
ChatGPT
Хорошо! Ниже приведен пример полного кода для GitHub Actions для сборки и деплоя проекта с использованием Gulp. Допустим, мы собираем статический сайт и хотим задеплоить его на GitHub Pages.

Создайте файл .github/workflows/gulp-ci.yml со следующим содержимым:
yaml
Copy code
name: Gulp CI/CD

on:
  push:
    branches:
      - main # Измените на вашу основную ветку

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14' # Измените на желаемую версию Node.js

      - name: Install project dependencies
        run: npm install

      - name: Build with Gulp
        run: gulp build # Замените на ваш таск для сборки проекта

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          publish_dir: ./app/index.html # Путь к каталогу, содержащему собранные файлы (измените, если требуется)
          deploy_key: ${{ secrets.GITHUB_TOKEN }}
